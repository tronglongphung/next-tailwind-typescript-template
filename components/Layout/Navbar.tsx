const navigation = [
  // { name: 'Giới Thiệu', href: '/about-us' },
];

function classNames(...classes) {
  return classes.filter(Boolean).join(' ');
}

export default function Navbar() {
  return <nav className="">add navigation here</nav>;
}
